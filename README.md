# API_PythonFunction

main:app --host 192.168.0.8 --port 8000 --reload

python.exe -m pip install --upgrade pip

pip install fastapi uvicorn SQLAlchemy pydantic passlib[bcrypt] python-jose[cryptography] email-validator
